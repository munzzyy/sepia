{
  "name": "sepia",
  "version": "0.4.0",
  "description": "Share images without oversharing. On-device metadata X-ray, real redaction, and proof the output is clean.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "e2e": "node test/e2e_app.mjs && node test/e2e_wrapper.mjs && node test/e2e_batch.mjs",
    "check": "bash tools/check-clean.sh",
    "fixtures": "node tools/gen-fixtures.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
